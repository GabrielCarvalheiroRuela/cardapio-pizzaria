<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Cardapiopizzaria" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:statusBarColor">@android:color/black</item> <!-- Define a barra de status como preta -->
    </style>

    <style name="Theme.Cardapiopizzaria" parent="Base.Theme.Cardapiopizzaria" />
</resources>
